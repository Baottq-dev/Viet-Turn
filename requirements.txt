# =============================================================================
# MM-VAP-VI Requirements (pinned for compatibility)
# =============================================================================
# Install order matters! Follow GUIDE.md Section 2.2 or use:
#   pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
# =============================================================================

# Core (torch/torchaudio installed separately with CUDA, see above)
numpy>=1.24.0
scipy>=1.10.0

# HuggingFace (pin BEFORE pyannote to prevent pip pulling 1.x)
huggingface_hub>=0.23.0,<1.0
transformers>=4.40.0,<5.0
accelerate>=0.30.0,<1.0

# Speaker Diarization
pyannote.audio>=3.1.0,<4.0

# Audio Processing
soundfile>=0.12.0
imageio-ffmpeg

# ASR
faster-whisper>=1.0.0

# ML Metrics
scikit-learn>=1.3.0

# Training
wandb>=0.16.0

# Data Pipeline
yt-dlp

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
tqdm>=4.66.0
matplotlib>=3.8.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
